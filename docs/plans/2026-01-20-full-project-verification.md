# Полная проверка проекта Budget App

**Дата создания:** 2026-01-20
**Цель:** Комплексная верификация проекта — production readiness, аудит после обновления зависимостей, проверка качества, подготовка к передаче

---

## Содержание

1. [Подготовка окружения](#1-подготовка-окружения)
2. [Проверка сборки и запуска](#2-проверка-сборки-и-запуска)
3. [Тестирование бизнес-логики](#3-тестирование-бизнес-логики)
4. [Проверка API endpoints и User workflows](#4-проверка-api-endpoints-и-user-workflows)
5. [Интеграционная проверка](#5-интеграционная-проверка)
6. [Аудит и очистка](#6-аудит-и-очистка)
7. [Документация](#7-документация)
8. [Порядок выполнения](#8-порядок-выполнения)

---

## 1. Подготовка окружения

### 1.1 Проверка prerequisites
- [ ] Node.js/Bun установлен и версия соответствует требованиям
- [ ] Docker и Docker Compose установлены
- [ ] Git репозиторий чистый (`git status` без uncommitted changes)

### 1.2 Настройка переменных окружения
- [ ] Скопировать `.env.example` → `.env` (если отсутствует)
- [ ] Заполнить обязательные переменные:
  - `DB_USER`, `DB_PASSWORD`
  - `JWT_ACCESS_SECRET` (минимум 32 символа)
  - `JWT_REFRESH_SECRET` (минимум 32 символа)
  - `RABBITMQ_URL`
- [ ] Проверить что секреты не закоммичены в git

### 1.3 Настройка Telegram-бота
- [ ] Создать бота через @BotFather в Telegram
- [ ] Получить `TELEGRAM_BOT_TOKEN`
- [ ] Добавить токен в `.env`
- [ ] Получить свой `chat_id` (через @userinfobot или подобный)
- [ ] Записать `chat_id` для тестирования уведомлений

### 1.4 Установка зависимостей
- [ ] `bun install` в корне проекта
- [ ] Проверить отсутствие ошибок и warnings
- [ ] `bun install` завершился без `npm audit` critical issues

---

## 2. Проверка сборки и запуска

### 2.1 Статические проверки (без запуска)
- [ ] `npm run typecheck` — TypeScript компиляция без ошибок
- [ ] `npm run lint` — ESLint без ошибок
- [ ] `npm run format` — Prettier форматирование применено

### 2.2 Development mode
- [ ] `make dev-infra` — запуск инфраструктуры (PostgreSQL, RabbitMQ)
- [ ] Дождаться готовности контейнеров (health checks)
- [ ] `npm run api:dev` — API сервис стартует без ошибок
- [ ] `npm run notifications:dev` — Notifications сервис стартует без ошибок
- [ ] Проверить логи на отсутствие ошибок/warnings
- [ ] `curl http://localhost:3000/health` — возвращает 200 OK
- [ ] `curl http://localhost:3001/health` — возвращает 200 OK
- [ ] Swagger UI доступен: `http://localhost:3000/docs`

### 2.3 Production build
- [ ] `npm run api:build` — сборка API без ошибок
- [ ] `npm run notifications:build` — сборка Notifications без ошибок
- [ ] Проверить что `dist/` папки созданы в обоих сервисах
- [ ] `node api/dist/main.js` — production bundle запускается

### 2.4 Docker production mode
- [ ] `make stop` — остановить dev контейнеры
- [ ] `make prod` — запуск production compose
- [ ] Все 6 контейнеров running и healthy
- [ ] `curl http://localhost:8080/api/health` — Nginx proxy работает
- [ ] Проверить логи всех контейнеров на ошибки
- [ ] `make stop-prod` — корректная остановка

---

## 3. Тестирование бизнес-логики

### 3.1 Unit-тесты
- [ ] `npm run api:test` — все тесты API проходят
- [ ] `cd notifications && npm run test` — все тесты Notifications проходят
- [ ] `make test-cov` — coverage report сгенерирован
- [ ] Проанализировать покрытие:
  - [ ] `api/src/modules/auth/` — покрытие критичных путей
  - [ ] `api/src/modules/transactions/` — покрытие бизнес-логики
  - [ ] `api/src/modules/budgets/` — покрытие лимитов и расчётов
  - [ ] `api/src/modules/goals/` — покрытие депозитов и прогресса
  - [ ] `api/src/modules/categories/` — покрытие CRUD

### 3.2 Проверка тестовой инфраструктуры
- [ ] Тесты изолированы (не зависят от порядка выполнения)
- [ ] Моки корректно очищаются между тестами
- [ ] Нет `skip` или `todo` тестов без причины
- [ ] Тесты не используют hardcoded таймауты (flaky tests)

### 3.3 Анализ непокрытых областей
- [ ] Выявить модули без тестов
- [ ] Выявить edge cases без покрытия:
  - Невалидные входные данные
  - Граничные значения (0, отрицательные, MAX_INT)
  - Пустые массивы/объекты
  - Null/undefined handling
- [ ] Задокументировать gaps для будущего исправления

---

## 4. Проверка API endpoints и User workflows

### 4.1 Auth модуль
- [ ] POST /auth/register — регистрация нового пользователя
- [ ] POST /auth/register — повторная регистрация (ошибка 409)
- [ ] POST /auth/login — успешный логин, получение токенов
- [ ] POST /auth/login — неверный пароль (ошибка 401)
- [ ] POST /auth/refresh — обновление access token
- [ ] POST /auth/refresh — истёкший refresh token (ошибка 401)
- [ ] POST /auth/logout — выход из системы
- [ ] Запросы без токена возвращают 401

### 4.2 Categories модуль
- [ ] GET /categories — список категорий (пустой для нового юзера)
- [ ] POST /categories — создание категории
- [ ] POST /categories — дубликат имени (проверка поведения)
- [ ] PUT /categories/:id — обновление категории
- [ ] PUT /categories/:id — несуществующий id (ошибка 404)
- [ ] DELETE /categories/:id — удаление категории
- [ ] DELETE /categories/:id — категория с транзакциями (проверка поведения)

### 4.3 Transactions модуль
- [ ] POST /transactions — создание income транзакции
- [ ] POST /transactions — создание expense транзакции
- [ ] GET /transactions — список с пагинацией
- [ ] GET /transactions — фильтрация по датам
- [ ] GET /transactions — фильтрация по категории
- [ ] GET /transactions/:id — получение конкретной транзакции
- [ ] PUT /transactions/:id — обновление транзакции
- [ ] DELETE /transactions/:id — удаление транзакции
- [ ] Валидация: отрицательная сумма, пустое описание, невалидная дата

### 4.4 Budgets модуль
- [ ] GET /budgets — список бюджетов
- [ ] GET /budgets/current — текущий бюджет (или 404 если нет)
- [ ] POST /budgets/current — создание бюджета на текущий период
- [ ] POST /budgets/current — повторное создание (проверка поведения)
- [ ] POST /budgets/current/limits — установка лимита на категорию
- [ ] POST /budgets/current/limits — лимит на несуществующую категорию
- [ ] DELETE /budgets/current/limits/:categoryId — удаление лимита
- [ ] DELETE /budgets/:id — удаление бюджета
- [ ] Проверка расчёта: расход по категории vs лимит

### 4.5 Goals модуль
- [ ] POST /goals — создание цели (название, целевая сумма, дедлайн)
- [ ] GET /goals — список целей с прогрессом
- [ ] GET /goals/:id — детали конкретной цели
- [ ] PUT /goals/:id — обновление цели
- [ ] POST /goals/:id/deposit — внесение средств
- [ ] POST /goals/:id/deposit — депозит больше оставшейся суммы
- [ ] DELETE /goals/:id — удаление цели
- [ ] Проверка расчёта прогресса (currentAmount / targetAmount)

### 4.6 Полные User workflows (E2E сценарии)
- [ ] **Сценарий "Новый пользователь":**
  Регистрация → Создание категорий → Создание бюджета → Добавление транзакций → Проверка баланса

- [ ] **Сценарий "Контроль расходов":**
  Установка лимита на категорию → Транзакции до лимита → Транзакция сверх лимита → Проверка предупреждения

- [ ] **Сценарий "Накопление на цель":**
  Создание цели → Несколько депозитов → Достижение цели → Проверка статуса

- [ ] **Сценарий "Месячный цикл":**
  Бюджет на январь → Транзакции → Новый месяц → Новый бюджет → История сохранена

---

## 5. Интеграционная проверка

### 5.1 Связка сервисов через Docker
- [ ] Все контейнеры в одной сети видят друг друга
- [ ] API подключается к postgres-api (проверить логи)
- [ ] Notifications подключается к postgres-notifications
- [ ] Оба сервиса подключаются к RabbitMQ
- [ ] Nginx проксирует запросы к API корректно

### 5.2 RabbitMQ events
- [ ] При создании транзакции событие публикуется в очередь
- [ ] При депозите на цель событие публикуется
- [ ] При превышении лимита бюджета событие публикуется
- [ ] Notifications сервис получает события из очереди
- [ ] Проверить RabbitMQ Management UI: `http://localhost:15672`
  - Очереди созданы
  - Сообщения обрабатываются (не накапливаются)
  - Нет dead-letter сообщений

### 5.3 Telegram уведомления
- [ ] Настроить notification settings для тестового пользователя
- [ ] Создать транзакцию → получить уведомление в Telegram
- [ ] Внести депозит на цель → получить уведомление
- [ ] Превысить лимит бюджета → получить предупреждение
- [ ] Отключить уведомления → проверить что не приходят

### 5.4 Database integrity
- [ ] Foreign keys работают (нельзя удалить связанные данные)
- [ ] Cascade delete работает где настроен
- [ ] Уникальные индексы предотвращают дубликаты
- [ ] Prisma миграции применяются без ошибок на чистой БД

---

## 6. Аудит и очистка

### 6.1 Поиск мёртвого кода
- [ ] Неиспользуемые экспорты (функции, типы, константы)
- [ ] Неиспользуемые импорты
- [ ] Закомментированный код
- [ ] Неиспользуемые переменные и параметры
- [ ] Пустые файлы или файлы-заглушки
- [ ] `console.log` / отладочный код в production коде

### 6.2 Устаревшие и лишние файлы
- [ ] Старые конфиги (webpack, babel, etc.) если не используются
- [ ] Забытые TODO-файлы, NOTES.md, scratch files
- [ ] Дубликаты конфигурации между сервисами
- [ ] Неиспользуемые скрипты в `/scripts`
- [ ] Устаревшие документы в `/docs/plans` (завершённые планы)
- [ ] Файлы `.bak`, `.old`, `.tmp`
- [ ] Неиспользуемые Docker-файлы или compose overrides

### 6.3 Оптимизация структуры папок
- [ ] Консистентность именования (camelCase vs kebab-case)
- [ ] Файлы в правильных директориях по DDD структуре
- [ ] `shared/` код вынесен корректно (без циклических зависимостей)
- [ ] Index файлы (barrel exports) используются консистентно
- [ ] Тесты рядом с кодом или в отдельной папке — но единообразно

### 6.4 Консолидация конфигов
- [ ] `tsconfig.base.json` переиспользуется корректно
- [ ] ESLint конфиг не дублируется между сервисами
- [ ] Prettier конфиг единый для всего проекта
- [ ] Docker ENV переменные не дублируются
- [ ] Общие константы вынесены (таймауты, лимиты, etc.)

### 6.5 Аудит зависимостей
- [ ] `npm outdated` — проверить устаревшие пакеты
- [ ] `npm audit` — проверить security vulnerabilities
- [ ] Неиспользуемые dependencies в package.json:
  - [ ] Корневой package.json
  - [ ] api/package.json
  - [ ] notifications/package.json
- [ ] DevDependencies которые должны быть в dependencies (и наоборот)
- [ ] Дублирующиеся зависимости между сервисами (кандидаты на вынос в root)
- [ ] Проверить lock-файл на консистентность

### 6.6 Проверка типизации
- [ ] Нет `any` типов без явной необходимости
- [ ] Нет `@ts-ignore` / `@ts-expect-error` без комментария причины
- [ ] Нет `as` type assertions где можно избежать
- [ ] Strict mode включён и соблюдается
- [ ] Zod схемы синхронизированы с TypeScript типами

### 6.7 Code style и консистентность
- [ ] Нет смешения стилей (async/await vs .then())
- [ ] Error handling единообразный
- [ ] Логирование через Pino везде (не console.log)
- [ ] Naming conventions соблюдаются:
  - Классы: PascalCase
  - Функции/переменные: camelCase
  - Константы: UPPER_SNAKE_CASE
  - Файлы: kebab-case или согласно convention проекта

---

## 7. Документация

### 7.1 README актуальность
- [ ] Описание проекта соответствует текущему состоянию
- [ ] Инструкция по установке работает (проверить на чистой системе)
- [ ] Список команд (`make dev`, `make prod`, etc.) актуален
- [ ] Переменные окружения в README совпадают с `.env.example`
- [ ] Архитектурная диаграмма (если есть) актуальна

### 7.2 API документация
- [ ] Swagger/OpenAPI описывает все endpoints
- [ ] Примеры запросов/ответов корректны
- [ ] Описания ошибок соответствуют реальным кодам
- [ ] Авторизация документирована (Bearer token)

### 7.3 Inline документация
- [ ] JSDoc на публичных функциях (где неочевидно)
- [ ] Комментарии к сложной бизнес-логике
- [ ] TODO/FIXME комментарии актуальны или удалены

### 7.4 Финальный отчёт
- [ ] Создать `docs/plans/2026-01-20-verification-report.md`:
  - Дата проверки
  - Версии зависимостей
  - Пройденные проверки ✅
  - Найденные проблемы ❌
  - Исправленные проблемы 🔧
  - Рекомендации на будущее
  - Список удалённых файлов
  - Production readiness: GO / NO-GO

---

## 8. Порядок выполнения

### Рекомендуемая последовательность:

```
1. Подготовка (блок 1)
   └── Без этого остальное не работает

2. Статические проверки (блок 2.1) — первым делом
   └── Быстро выявляет базовые проблемы

3. Dev mode запуск (блок 2.2) — основа для тестирования
   └── Нужен работающий dev для проверки API

4. Unit-тесты (блок 3) — параллельно с dev
   └── Можно запускать независимо

5. API endpoints (блок 4) — требует работающий dev
   └── Ручное или автоматизированное тестирование

6. Production build (блок 2.3-2.4) — после успеха dev
   └── Проверяем что prod собирается

7. Интеграция (блок 5) — требует оба сервиса
   └── Telegram бот, RabbitMQ events

8. Аудит и очистка (блок 6) — независимый
   └── Можно делать параллельно с тестированием

9. Документация (блок 7) — в конце
   └── Фиксируем результаты
```

### Критерии успеха всей проверки:

| Критерий | Must Have | Nice to Have |
|----------|-----------|--------------|
| TypeScript без ошибок | ✅ | |
| ESLint без ошибок | ✅ | |
| Unit-тесты проходят | ✅ | |
| Dev mode работает | ✅ | |
| Prod mode работает | ✅ | |
| Health checks отвечают | ✅ | |
| Основные API endpoints | ✅ | |
| Все edge cases | | ✅ |
| Telegram уведомления | | ✅ |
| 100% покрытие тестами | | ✅ |
| Нет мёртвого кода | | ✅ |

---

## Результаты проверки

> Заполняется по мере выполнения

### Статус: 🔄 В процессе / ✅ Завершено / ❌ Заблокировано

**Дата начала:** _______________
**Дата завершения:** _______________

### Найденные проблемы:

| # | Блок | Проблема | Критичность | Статус |
|---|------|----------|-------------|--------|
| 1 | | | | |
| 2 | | | | |

### Удалённые файлы:

```
- (список файлов)
```

### Production Readiness:

- [ ] **GO** — проект готов к production
- [ ] **NO-GO** — требуются исправления (см. список проблем)
